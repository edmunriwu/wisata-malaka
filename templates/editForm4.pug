html
  head
    title WISATA ALAM DI NTT
    meta(name="viewport" content="width=device-width, initial-scale=1")
    link(href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.2/dist/css/bootstrap.min.css" rel="stylesheet")
    script(src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.2/dist/js/bootstrap.bundle.min.js")
    link(rel="stylesheet" href="./bg/bg.css")
  body
  div(class="container p-4 my-4 bg-white text-dark")
    div(id="demo" class="carousel slide" data-bs-ride="carousel")
      div(class="carousel-indicators")
        button(type="button" data-bs-target="#demo" data-bs-slide-to="0" class="active")
        button(type="button" data-bs-target="#demo" data-bs-slide-to="1")
        button(type="button" data-bs-target="#demo" data-bs-slide-to="2")
        button(type="button" data-bs-target="#demo" data-bs-slide-to="3")             
      div(class="carousel-inner")
        div(class="carousel-item active")
          img(src="./gambar/pn3.jpeg" width="100%" height="400")
        div(class="carousel-item")
          img(src="./gambar/pn2.jpg" width="100%" height="400")
        div(class="carousel-item")
          img(src="./gambar/pn1.jpg" width="100%" height="400")
        div(class="carousel-item")
          img(src="./gambar/pn4.jpg" width="100%" height="400")
      button(class="carousel-control-prev" type="button" data-bs-target="#demo" data-bs-slide="prev")
        span(class="carousel-control-prev-icon")
      button(class="carousel-control-next" type="button" data-bs-target="#demo" data-bs-slide="next")
        span(class="carousel-control-next-icon")
    nav(class="navbar navbar-expand-sm navbar-dark")
      div(class="container-fluid")
          a(class="navbar-brand" href="/")
            img(src="./gambar/logo.jpg" alt="Avatar Logo" style="width:40px;" class="rounded-pill")
          button(class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar")
            span(class="navbar-toggler-icon")
          div(class="collapse navbar-collapse" id="collapsibleNavbar")
            ul(class="navbar-nav")
              li(class="nav-item")
                a(class="nav-link" class="text-body" href="/list4") Kembali
              li(class="nav-item")
                a(class="nav-link" class="text-body" href="/main") Beranda
              li(class="nav-item")
                a(class="nav-link" class="text-body" href="/") Logout
    div(class="row")
      div(class="col-sm-5") 
        h2 Petunjuk :
        center 
          img(src="./gambar/edit.jpeg" width="100%" height="260")
        hr
        div(class="toast show")
        div(class="toast-header")
          strong(class="me-auto") Change the data according to the conditions
          button(type="button" class="btn-close" data-bs-dismiss="toast")
        div(class="toast-body")
          p 1. Id cannot be changed
          p 2. Make sure the name of the inn is correct
          p 3. Make sure the location is accurate
          p 4. Fill in the Appropriate Nightly Fee
          p 5. Make sure the numbMake er of the inn is active
          p 6. Make sure the Lodging Email is active and CORRECT
          p 7. Make sure the operating hours are appropriate
      div(class="col-sm-7 mx-auto")
        div(class="alert alert-info")
          h2 Ubah Data
        form(action="/edit4" method="post" enctype="multipart/form-data")
          div(class="input-group mb-2")
            span(class="input-group-text") ID Penginapan
            input(type="number" name="id_penginapan" class="form-control" value=penginapan.id_penginapan required)
          div(class="input-group mb-2")
            span(class="input-group-text") NAMA penginapan
            input(type="text" name="nama_penginapan" class="form-control" value=penginapan.nama_penginapan required)
          div(class="input-group mb-2")
            span(class="input-group-text") Lokasi Penginapan
            input(type="text" name="lokasi_penginapan" class="form-control" value=penginapan.lokasi_penginapan required)
          div(class="input-group mb-2")
            span(class="input-group-text") Biaya Permalam
            input(type="text" name="biaya" class="form-control" value=penginapan.biaya required)
          div(class="input-group mb-2")
            span(class="input-group-text") TELEPON
            input(type="number" name="telepon" class="form-control" value=penginapan.telepon required)
          div(class="input-group mb-2")
            span(class="input-group-text") EMAIL
            input(type="email" name="email" class="form-control" value=penginapan.email required)
          div(class="input-group mb-2")
            span(class="input-group-text") Jam
            input(type="text" name="jam" class="form-control" value=penginapan.jam required)
          span(class="input-group-text") Gambar
          img(src="/penginapgbr/" + penginapan.gambar width="100%")
          input(type="file" name="userfile" class="form-control" value=penginapan.gambar required)
          div(style="margin-top: 10px;")
            input(type="submit" value="Simpan" class="btn btn-success")
            input(type="button", value="Batal" onClick="javascript:history.go(-1);" class="btn btn-info")
